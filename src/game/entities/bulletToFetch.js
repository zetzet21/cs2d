export default class BulletToFetch {
    constructor(x, y, rotation, uniqId) {
        this.x = x;
        this.y = y;
        this.rotation = rotation;
        this.uniqId = uniqId;
        this.status = "new";
    }
}